export default {
  darkMode: 'class', // not 'media'
  content: [
    './src/**/*.{html,js,svelte,ts}', // covers your app
    './src/app.css', // make sure this is included if styles are only in app.css
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
